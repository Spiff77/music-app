# Music APP

Very simple express app.

## Load db
`mysql -u root -h127.0.0.1 -p < script.sql`

## Instructions
- You need to do `npm start` in both front-end and back-end folder (don't forget to install dependencies first!)
- Access then to `http://localhost:8082`
